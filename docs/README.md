# Documentation

This folder holds project documentation:

- `Project-Proposal.pdf` — initial proposal / scope document
- `SRS.pdf` — Software Requirements Specification
- `API-Documentation.pdf` — exported API reference (see also live Swagger UI at `/swagger-ui.html`)
- `Database-Schema.pdf` — ER diagram / schema reference
- `Screenshots/` — UI screenshots used in the proposal/report

These are placeholders — replace with your actual generated documents.
